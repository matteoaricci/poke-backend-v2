<%= form_with(model: poke_on_team, local: true) do |form| %>
  <% if poke_on_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poke_on_team.errors.count, "error") %> prohibited this poke_on_team from being saved:</h2>

      <ul>
        <% poke_on_team.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
